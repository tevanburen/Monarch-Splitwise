/**
 * UI providers for the extension's React application.
 *
 * Exports:
 * - RuntimeStateProvider: Synchronizes UI state with background service worker
 * - useRuntimeStateContext: Hook to access runtime state in components
 */
export * from "./RuntimeStateProvider";
